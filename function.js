//one winner
let player1 = prompt("what's ya name?");
let player2 = prompt("what's ya name?");
let symbol1 = "X";
let symbol2 = "O";
let turn = 0;
let anchor = document.getElementsByTagName("a")
//randomly selects a player to go first
let picker = Math.floor(Math.random() * 100);
//players take turns
if (turn % 2 === 0) {
  //replace the contents of a node with the current players name.
};
//one symbol per turn

//three in a row wins

//if full, no winner

//cant click in a space taken

